class Transistor extends Element{

    constructor(adder, parent){

        super(Transistor, 97, adder, "res/transistor.png", parent);

        // var elem = this.elem;

        // elem.height = 97;

        // elem.onmouseup = function(e){

        //     if(adder){

        //         new Transistor(true, parent);

        //         adder = false;

        //     }
        //     if(!adder)if(parseInt(this.style.left.split("px")[0], 10) <= 160)this.remove();

        //     document.onmousemove = null;

        // };

    }

}
