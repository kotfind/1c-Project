class Switch extends Element{

    constructor(adder, parent){

        super(Switch, 40, adder, "res/switchOff.png", parent);

        // var elem = this.elem;

        // elem.height = 40;

        // elem.onmouseup = function(e){

        //     if(adder){

        //         new Switch(true, parent);

        //         adder = false;

        //     }
        //     if(!adder)if(parseInt(this.style.left.split("px")[0], 10) <= 160)this.remove();

        //     document.onmousemove = null;

        // };

    }

}
