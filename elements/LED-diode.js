class LED_Diode extends Element{

    constructor(adder, parent){

        super(LED_Diode, 40, adder, "res/LED-diode.png", parent);

        // var elem = this.elem;

        // elem.height = 40;

        // elem.onmouseup = function(e){

        //     if(adder){

        //         new LED_Diode(true, parent);

        //         adder = false;

        //     }
        //     if(!adder)if(parseInt(this.style.left.split("px")[0], 10) <= 160)this.remove();

        //     document.onmousemove = null;

        // };

    }

}
